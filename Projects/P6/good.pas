program example(input, output);
var x, y: integer;
function gcd(a, b: integer): integer;
begin
integer alpha = 12
real beta = 12.3456
real gamma = 123.456E+7
real other = 3.4E+
if b = 0 then gcd := a
else gcd := gcd(b, a mod b)
end;

begin
read(x, y);
write(gcd(x, y))
end.
