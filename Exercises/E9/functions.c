#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/* Declare your functions above your main
 * 
 */

// create a function that adds 2 ints and returns 1 int.

// create a function that multiplies two reals and returns 1 real

// create a function that takes in a string and an int. then will copy the string that many times.
// ex: stringMultiply("str", 4) will return "strstrstrstr".

// create a funcion that takes in a string and returns the 3rd Character. (read: char)

// create a function that takes in a string and a character. This will return the index (int).
// ex: checkIndex("string", "r") will return 3.

// create a function that combines two strings and returns 1 string

// create a function that takes in two number arrays and returns the combination of them both

// create a function that

int main()
{
    // any variables you may use

    // Call your functions here. Hard code any of the parameters to any value you choose.

    return 0;
}
